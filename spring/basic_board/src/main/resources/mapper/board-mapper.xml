<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.basic_board.mapper.BoardMapper">

    <resultMap id="board" type="com.green.basic_board.dto.BoardDTO">
        <id column="BOARD_NUM"   property="boardNum"/>
        <result column="TITLE"       property="title"/>
        <result column="WRITER"      property="writer"/>
        <result column="CONTENT"     property="content"/>
        <result column="READ_CNT"    property="readCcnt"/>
        <result column="CREATE_DATE" property="createDate"/>
    </resultMap>

    <select id="test1" resultType="String">
        SELECT TITLE
        FROM BASIC_BOARD
        WHERE BOARD_NUM = 1
    </select>

    <select id="test2" resultType="int">
        SELECT BOARD_NUM
        FROM BASIC_BOARD
    </select>

    <select id="test3" resultMap="board">
        SELECT BOARD_NUM
        , TITLE
        , CONTENT
        , WRITER
        FROM BASIC_BOARD
        WHERE BOARD_NUM = #{boardNum}
    </select>

    <select id="test4" resultMap="board">
        SELECT BOARD_NUM
        , TITLE
        , WRITER
        , CREATE_DATE
        FROM BASIC_BOARD
        WHERE BOARD_NUM &gt; #{boardNum}
        AND READ_CNT &lt; #{readCnt}
    </select>

    <insert id="insertBoard">
        INSERT INTO BASIC_BOARD (
            TITLE
            , WRITER
            , CONTENT
        ) VALUES (
         #{title}
        , #{writer}
        , #{content}
        )
    </insert>

    <!-- 컨트롤 쉬프트 슬러시( 라인맞춰서 주석)-->
    <!--하나의 게시글을 삭제하는 쿼리 작성 및 기능 구현, 실행은 postman에서!-->
    <delete id="deleteBoard">
        DELETE FROM BASIC_BOARD
        WHERE BOARD_NUM = #{boardNum}
    </delete>

    <update id="updateBoard">
        UPDATE BASIC_BOARD
        SET
            TITLE = #{title}
          , CONTENT = #{content}
        WHERE BOARD_NUM = #{boardNum}
    </update>
</mapper>































