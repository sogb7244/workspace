<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.example.restfulAPI_test.cateCar.mapper.CarsCateMapper">
    <resultMap id="categories" type="com.example.restfulAPI_test.cateCar.dto.CarsCateDTO">
        <id     column="CATE_NUM"   property="cateNum"/>
        <result column="CATE_NAME"  property="cateName"/>
    </resultMap>

    <select id="selectCate" resultMap="categories">
        SELECT *
        FROM CARS_CATE
        ORDER BY CATE_NUM
    </select>

</mapper>































